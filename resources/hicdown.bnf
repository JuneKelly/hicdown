Document = (nl | (dnl)*) Block (dnl Block)* (nl | (dnl)*)
Block = (TextRun | Segment)+

spc = #'\s'
dnl = #'\n(\n)+'
nl = !dnl #'\n'

TextRun = (textchar | escape | nl)+
textchar = #'[^\n\[\]]'

escape = '\\[' | '\\]'

Segment = '[' tag spc+ TextRun+ ']'
tag = #':[^\s]+'
